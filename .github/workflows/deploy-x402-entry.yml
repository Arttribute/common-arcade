name: Deploy testnet x402 seat entry
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: Type deploy to deploy the reviewed x402-entry-v4 escrow on the EIP-3009 testnets
        required: true
        type: string
      resume_run:
        description: Previous run ID whose pending deployment receipts must be reconciled
        required: false
        type: string
permissions:
  contents: read
  actions: read
concurrency:
  group: deploy-testnet-x402-entry
  cancel-in-progress: false
jobs:
  deploy:
    if: github.ref == 'refs/heads/main' && inputs.confirm == 'deploy'
    environment: development
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        network: [base-sepolia, arc-testnet, celo-sepolia]
    steps:
      - uses: actions/checkout@v7
      - name: Require receipt reconciliation for retries
        env:
          ATTEMPT: ${{ github.run_attempt }}
        run: test "$ATTEMPT" = 1
      - uses: pnpm/action-setup@v6
        with:
          version: 9.15.3
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.5.1
      - run: bash packages/contracts/setup.sh
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @common-arcade/economy... build
      - run: forge build --root packages/contracts
      - uses: actions/download-artifact@v4
        if: inputs.resume_run != ''
        with:
          name: x402-entry-${{ matrix.network }}
          path: packages/contracts/deployments/x402-entry-v4
          run-id: ${{ inputs.resume_run }}
          github-token: ${{ github.token }}
      - name: Deploy and verify escrow
        env:
          ARCADE_TESTNET_ADMIN: '0xD9303DFc71728f209EF64DD1AD97F5a557AE0Fab'
          ARCADE_DEPLOYER_KEY: ${{ secrets.ARCADE_TESTNET_AUDIT_KEY }}
        run: node services/payment-service/scripts/deploy-testnet.mjs "${{ matrix.network }}" --revision=x402-entry-v4 --broadcast
      - name: Preserve receipts including pending transactions
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: x402-entry-${{ matrix.network }}
          path: packages/contracts/deployments/x402-entry-v4/${{ matrix.network }}.json
