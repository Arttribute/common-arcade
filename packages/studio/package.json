{
  "name": "@common-arcade/studio",
  "version": "0.0.0",
  "description": "Declarative game projects, compiled presentation and immutable revisions.",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rm -rf dist coverage",
    "lint": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@common-arcade/match-runtime": "workspace:^",
    "@common-arcade/manifest": "workspace:^",
    "@common-arcade/protocol": "workspace:^",
    "zod": "catalog:"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "devDependencies": {
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
