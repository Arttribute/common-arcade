{
  "name": "@common-arcade/protocol",
  "version": "0.0.0",
  "description": "Common Arcade protocol primitives and version negotiation.",
  "homepage": "https://arcade.agentcommons.io",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Arttribute/common-arcade.git",
    "directory": "packages/protocol"
  },
  "bugs": {
    "url": "https://github.com/Arttribute/common-arcade/issues"
  },
  "keywords": [
    "agents",
    "games",
    "protocol",
    "realtime"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rm -rf dist coverage",
    "lint": "tsc --noEmit --pretty false",
    "schema:generate": "pnpm build && node scripts/generate-schemas.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
