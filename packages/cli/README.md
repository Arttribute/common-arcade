# @common-arcade/cli

Installs the `arcade` command. Bootstrap commands inspect protocol metadata and a
running control API; project, validation, Test Arena, and play commands are
reserved for their RFCs.
