{
  "name": "@common-arcade/manifest",
  "version": "0.0.0",
  "description": "Common Arcade game manifest model and validation.",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rm -rf dist coverage",
    "lint": "tsc --noEmit --pretty false",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "devDependencies": {
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
