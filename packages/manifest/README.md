# @common-arcade/manifest

Reserved for the signed, portable game manifest model. No validator is exposed
until the manifest RFC defines identity, capabilities, artifacts, permissions,
presentation, compatibility, and provenance.
