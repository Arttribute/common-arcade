---
'@common-arcade/protocol': minor
'@common-arcade/control-client': minor
'@common-arcade/cli': minor
'@common-arcade/sdk': patch
---

Unify managed-game authoring around the versioned document schema, scaffold live rules by default, add headless runtime tests and match abandonment, and report unavailable upstream services clearly. Support declared roles and late joining across game genres.
