# Changesets

Run `pnpm changeset` for every user-visible change to a publishable package. Apps,
services, and infrastructure are intentionally excluded from npm versioning.

Publishing is disabled until npm trusted publishers and the protected `npm`
GitHub environment are configured. See `docs/operations/releases.md`.
