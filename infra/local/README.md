# Local infrastructure

The bootstrap surfaces run without local cloud dependencies. Add emulators or
containers only when a Phase 0 proof requires them; do not let emulator behavior
silently define the protocol.
