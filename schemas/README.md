# Schemas

Generated JSON Schema 2020-12 artifacts for the incubating protocol are stored
under `v0alpha1`. They are generated from `@common-arcade/protocol` with:

```sh
pnpm --filter @common-arcade/protocol schema:generate
```

The index records a canonical-content digest for every schema. The schemas remain
incubating until the protocol compatibility gates in the system design are met.
